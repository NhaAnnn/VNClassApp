class PackageData {
  final String? nameP; // Tham số không bắt buộc
  final dynamic agrReq; // Tham số không bắt buộc
  final dynamic agr2; // Tham số không bắt buộc
  final dynamic agr3; // Tham số không bắt buộc
  final dynamic agr4; // Tham số không bắt buộc
  final dynamic agr5; // Tham số không bắt buộc

  // Khởi tạo với tất cả tham số không bắt buộc
  PackageData({
    this.nameP,
    this.agrReq,
    this.agr2,
    this.agr3,
    this.agr4,
    this.agr5,
  });
}
